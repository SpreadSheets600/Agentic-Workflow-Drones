[project]
name = "agentic-workflow-drones"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "sentence-transformers>=6.0.1",
    "scikit-learn>=1.3",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["app/tests"]
